apiVersion: apps/v1
kind: Deployment
metadata:
  name: llm-deepseek-coder-v3-moe
  namespace: llm
  labels:
    app.kubernetes.io/part-of: localllm-model
    app.kubernetes.io/name: llm-deepseek-coder-v3-moe
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: llm-deepseek-coder-v3-moe
  template:
    metadata:
      labels:
        app: llm-deepseek-coder-v3-moe
        app.kubernetes.io/part-of: localllm-model
    spec:
      containers:
      - name: vllm
        image: vllm/vllm-openai:latest
        args:
          - "--model=deepseek-ai/DeepSeek-Coder-V2-Lite-Instruct"
          - "--served-model-name=DeepSeek-Coder-V2-Lite-Instruct"
          - "--host=0.0.0.0"
          - "--port=8000"
          - "--gpu-memory-utilization=0.78"
          - "--max-model-len=4096"
          - "--max-num-seqs=2"
          - "--enable-prefix-caching"
          - "--enable-auto-tool-choice"
          - "--tool-call-parser"
          - "deepseek_v3"
          - "--enable-chunked-prefill"
          - "--dtype"
          - "bfloat16"
          - "--trust-remote-code"
          - "--enforce-eager"
        ports:
          - containerPort: 8000
        resources:
          limits:
            nvidia.com/gpu: 1
        env:
          - name: VLLM_API_KEY
            valueFrom:
              secretKeyRef:
                name: llm-api-key
                key: API_KEY
          - name: HUGGING_FACE_HUB_TOKEN
            valueFrom:
              secretKeyRef:
                name: hf-token
                key: HF_TOKEN
          - name: HF_HOME
            value: "/data/hf"
          - name: HUGGINGFACE_HUB_CACHE
            value: "/data/hf/hub"
          - name: TRANSFORMERS_CACHE
            value: "/data/hf/transformers"
          - name: OMP_NUM_THREADS
            value: "8"
          - name: OMP_SCHEDULE
            value: "dynamic"
        volumeMounts:
          - name: hf-cache
            mountPath: /data/hf
      volumes:
        - name: hf-cache
          persistentVolumeClaim:
            claimName: llm-model-cache
---
apiVersion: v1
kind: Service
metadata:
  name: llm-active
  namespace: llm
  labels:
    app.kubernetes.io/part-of: localllm-model
spec:
  selector:
    app: llm-deepseek-coder-v3-moe
  ports:
    - port: 80
      targetPort: 8000
